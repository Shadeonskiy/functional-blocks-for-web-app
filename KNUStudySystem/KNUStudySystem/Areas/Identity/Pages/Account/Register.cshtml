@page
@model RegisterModel

<link rel="stylesheet" href="~/css/authorization.css" asp-append-version="true" />

<main class="set-center">
    <div class="register">
        <form asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <h2 class = "login__namer">Registration</h2>
            <div class="wrapper set-center">  
                <input class="login__field" asp-for="Input.FirstName" value="" aria-labelledby="placeholder"/>
                <label id="placeholder" class = "placeholder-input" asp-for="Input.FirstName">
                    <div class="placeholder-input-text">Your FirstName</div>
                </label> 
            </div>  
            <div class="wrapper set-center">  
                <input class="login__field" asp-for="Input.LastName" value="" aria-labelledby="placeholder"/>
                <label id="placeholder" class = "placeholder-input" asp-for="Input.LastName">
                    <div class="placeholder-input-text">Your LastName</div>
                </label>
            </div>  
            <div class="wrapper set-center">
                <input class="login__field" asp-for="Input.Email" value="" aria-labelledby="placeholder"/>
                <label id="placeholder" class = "placeholder-input" asp-for="Input.Email">
                    <div class="placeholder-input-text">Your Email</div>
                </label>
            </div>
            <div class="wrapper set-center">
                <input class="login__field" asp-for="Input.Password" value="" aria-labelledby="placeholder"/>
                <label id="placeholder" class = "placeholder-input" asp-for="Input.Password">
                    <div class="placeholder-input-text">Your Password</div>
                </label>
            </div>
            <div class="wrapper set-center">
                <input class="login__field" asp-for="Input.ConfirmPassword" value="" aria-labelledby="placeholder"/>
                <label id="placeholder" class = "placeholder-input" asp-for="Input.ConfirmPassword">
                    <div class="placeholder-input-text">Confirm your Password</div>
                </label>
            </div>
            <div class="wrapper set-center">
                <input class = "set-shadow-white register__field-submit pointer" type="submit" value="Sign up" required>
            </div>
        </form>
    </div>
    <div asp-validation-summary="All" class="text-danger set-center"></div>
</main>
@*<div class="col-md-6 col-md-offset-2">
    <section>
        <h4>Use another service to register.</h4>
        <hr />
        @{
            if ((Model.ExternalLogins?.Count ?? 0) == 0)
            {
                <div>
                    <p>
                        There are no external authentication services configured. See <a href="https://go.microsoft.com/fwlink/?LinkID=532715">this article</a>
                        for details on setting up this ASP.NET application to support logging in via external services.
                    </p>
                </div>
            }
            else
            {
                <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                    <div>
                        <p>
                            @foreach (var provider in Model.ExternalLogins)
                            {
                                <button type="submit" class="btn btn-primary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.DisplayName</button>
                            }
                        </p>
                    </div>
                </form>
            }
        }
    </section>
</div>*@

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/login.js"></script>
}
